---
import BaseLayout from '../layouts/BaseLayout.astro';

const REPO = 'https://github.com/wample0105/apo-once-glance';
const INSTALL_CMD =
  'curl -fsSL --retry 3 https://github.com/wample0105/apo-once-glance/releases/latest/download/install.sh | bash';

const clients = [
  'Claude Desktop',
  'Claude Code',
  'Cursor',
  'Codex',
  'ZCode',
  '…等支持 MCP 的客户端',
];
---

<BaseLayout
  title="下载安装 · 定影 OnceGlance"
  description="下载定影 OnceGlance：Windows 客户端解压即用，一条命令安装 CLI 与 Agent Skill，客户端内一键接入 MCP。零 API Key、零云端上传、零遥测。"
>
  <section class="dl-hero">
    <div class="container">
      <h1>下载安装</h1>
      <p class="dl-sub">
        客户端装一次，GUI、CLI、MCP 三种用法都有了。
      </p>
      <p class="dl-meta">当前版本 v0.1.1 · Windows 10 (19045+) / Windows 11 · MIT 开源</p>
    </div>
  </section>

  <section class="dl-body">
    <div class="container container-narrow">
      {/* 1 客户端 */}
      <article class="dl-block">
        <div class="block-head">
          <span class="block-no" aria-hidden="true">1</span>
          <div>
            <h2>客户端（GUI）</h2>
            <p>截图、标注、长截图、贴图、历史，都在这里。</p>
          </div>
        </div>
        <div class="block-body">
          <a class="btn btn-primary" href={`${REPO}/releases`} target="_blank" rel="noopener noreferrer">
            前往 Releases 下载
          </a>
          <p class="note">
            下载最新的 <code>onceglance</code> 客户端安装包，<strong>解压即用</strong>，无需安装器。
            需要 WebView2 Runtime（Windows 11 自带）。
          </p>
        </div>
      </article>

      {/* 2 CLI 与 Skill */}
      <article class="dl-block">
        <div class="block-head">
          <span class="block-no" aria-hidden="true">2</span>
          <div>
            <h2>CLI 与 Agent Skill（一条命令）</h2>
            <p>给 Agent 用的 <code>once</code> 命令行 + 教程生成 Skill。</p>
          </div>
        </div>
        <div class="block-body">
          <div class="cmd-box" role="group" aria-label="一键安装命令">
            <code>{INSTALL_CMD}</code>
            <button class="copy-btn" data-copy={INSTALL_CMD} aria-label="复制安装命令">复制</button>
          </div>
          <p class="note">
            在 Git Bash / WSL 中执行。脚本会自动按平台下载 <code>once</code> CLI 并校验
            SHA-256，安装到 <code>~/.onceglance/bin</code>，同时把 <code>onceglance-tutorial</code>
            Skill 装入通用 Agent Skills 目录（需 v0.1.2+）。
          </p>
        </div>
      </article>

      {/* 3 MCP 接入 */}
      <article class="dl-block">
        <div class="block-head">
          <span class="block-no" aria-hidden="true">3</span>
          <div>
            <h2>MCP 接入</h2>
            <p>让 Claude / Cursor 等 Agent 直接调用定影。</p>
          </div>
        </div>
        <div class="block-body">
          <p class="note">
            推荐在客户端内一键完成：打开主面板「Agent」页，对下面的客户端点击「一键接入」，自动写入
            MCP 配置（写入前自动备份原配置）：
          </p>
          <div class="client-chips">
            {clients.map((c) => (
              <span>{c}</span>
            ))}
          </div>
          <p class="note">
            也可以手动把 <code>once mcp</code> 加进你的 MCP 配置。完整步骤见
            <a href="/docs/agent">接入 AI Agent 教程</a>。
          </p>
        </div>
      </article>

      {/* 验证 */}
      <article class="dl-block verify">
        <h2>安装后验证</h2>
        <div class="cmd-box plain" role="group" aria-label="验证命令">
          <code>once status --json</code>
        </div>
        <p class="note">
          能看到实例、落盘、OCR、热键状态即安装成功。更多命令见
          <a href="/docs/appendix">附录 CLI 速查</a>。
        </p>
      </article>

      {/* 卸载 */}
      <article class="dl-block uninstall">
        <h2>卸载</h2>
        <ol class="uninstall-list">
          <li>客户端：退出托盘程序，删除程序目录；落盘图片在 <code>图片\Onceglance</code>，按需保留。</li>
          <li>CLI：删除 <code>~/.onceglance</code> 目录。</li>
          <li>MCP 配置：在客户端「Agent」页对已接入的客户端点击「移除」。</li>
        </ol>
      </article>
    </div>
  </section>

  <script>
    document.querySelectorAll('.copy-btn').forEach((btn) => {
      btn.addEventListener('click', async () => {
        try {
          await navigator.clipboard.writeText(btn.getAttribute('data-copy') ?? '');
          const prev = btn.textContent;
          btn.textContent = '已复制';
          setTimeout(() => (btn.textContent = prev), 1600);
        } catch {
          /* 剪贴板不可用时静默 */
        }
      });
    });
  </script>
</BaseLayout>

<style>
  .dl-hero {
    padding: 72px 0 40px;
    background:
      radial-gradient(900px 320px at 50% -120px, var(--accent-bg) 0%, transparent 65%),
      var(--bg-primary);
  }

  .dl-hero h1 {
    font-size: clamp(30px, 4vw, 42px);
    margin-bottom: 12px;
  }

  .dl-sub {
    font-size: 16.5px;
    color: var(--text-secondary);
  }

  .dl-meta {
    margin-top: 10px;
    font-size: 13px;
    color: var(--text-tertiary);
  }

  .dl-body {
    padding: 32px 0 96px;
  }

  .container-narrow {
    max-width: 760px;
  }

  .dl-block {
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    padding: 30px 32px;
    margin-bottom: 20px;
  }

  .block-head {
    display: flex;
    gap: 16px;
    align-items: flex-start;
    margin-bottom: 18px;
  }

  .block-no {
    flex-shrink: 0;
    width: 28px;
    height: 28px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--accent);
    color: #fff;
    border-radius: 50%;
    font-size: 14px;
    font-weight: 600;
    margin-top: 3px;
  }

  .block-head h2 {
    font-size: 20px;
    margin-bottom: 4px;
  }

  .block-head p {
    font-size: 14px;
    color: var(--text-secondary);
  }

  .block-body .note {
    margin-top: 14px;
    font-size: 14.5px;
    color: var(--text-secondary);
  }

  .note a {
    color: var(--accent-text);
  }

  .cmd-box {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 14px 14px 18px;
    background: var(--ov-surface);
    border: 1px solid var(--ov-surface-2);
    border-radius: var(--radius-btn);
  }

  .cmd-box.plain {
    padding: 12px 18px;
  }

  .cmd-box code {
    color: var(--ov-fg-dim);
    font-size: 12.5px;
    flex: 1;
    overflow-x: auto;
    white-space: nowrap;
    scrollbar-width: none;
  }

  .cmd-box.plain code {
    color: var(--ov-fg);
  }

  .copy-btn {
    flex-shrink: 0;
    padding: 5px 14px;
    background: var(--ov-surface-2);
    color: var(--ov-fg);
    border: 1px solid var(--ov-border);
    border-radius: 6px;
    font-size: 12.5px;
    cursor: pointer;
  }

  .copy-btn:hover {
    border-color: rgba(255, 255, 255, 0.3);
  }

  .client-chips {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin: 16px 0 4px;
  }

  .client-chips span {
    padding: 6px 14px;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 999px;
    font-size: 13.5px;
  }

  .verify,
  .uninstall {
    background: var(--bg-secondary);
  }

  .verify h2,
  .uninstall h2 {
    font-size: 17px;
    margin-bottom: 14px;
  }

  .verify .note {
    margin-top: 12px;
    font-size: 14.5px;
    color: var(--text-secondary);
  }

  .verify .note a {
    color: var(--accent-text);
  }

  .uninstall-list {
    margin: 0;
    padding-left: 20px;
    font-size: 14.5px;
    color: var(--text-secondary);
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
</style>
